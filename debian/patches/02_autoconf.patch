# Description: the lpi changes require a configure update
# UbuntuSpecific: launchpad
#
diff -Nur -x '*.orig' -x '*~' eog-2.26.1/configure eog-2.26.1.new/configure
--- eog-2.26.1/configure	2009-04-13 20:50:01.000000000 +0200
+++ eog-2.26.1.new/configure	2009-04-14 00:18:00.000000000 +0200
@@ -16193,6 +16193,7 @@
              gio-2.0 >= $GLIB_REQUIRED \
              gnome-desktop-2.0 >= $GNOME_DESKTOP_REQUIRED \
              gdk-pixbuf-2.0 >= $GDKPIXBUF_REQUIRED \
+	     launchpad-integration \
              gconf-2.0 >= $GCONF_REQUIRED \
              libart-2.0 >= $LIBART_REQUIRED \
              gtk+-unix-print-2.0 >= $GTK_PRINT_REQUIRED \
