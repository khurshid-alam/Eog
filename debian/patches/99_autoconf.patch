diff -Nur -x '*.orig' -x '*~' eog-2.29.91/configure eog-2.29.91.new/configure
--- eog-2.29.91/configure	2010-02-22 23:13:32.000000000 +0100
+++ eog-2.29.91.new/configure	2010-03-03 21:12:16.000000000 +0100
@@ -13673,9 +13673,11 @@
 EOG_MODULES="gtk+-2.0 >= $GTK_REQUIRED \
              glib-2.0 >= $GLIB_REQUIRED \
              gio-2.0 >= $GLIB_REQUIRED \
+             gio-unix-2.0 >= $GLIB_REQUIRED \
              gthread-2.0 >= $GLIB_REQUIRED \
              gnome-desktop-2.0 >= $GNOME_DESKTOP_REQUIRED \
              gdk-pixbuf-2.0 >= $GDKPIXBUF_REQUIRED \
+	     launchpad-integration \
              gconf-2.0 >= $GCONF_REQUIRED \
              gtk+-unix-print-2.0 >= $GTK_PRINT_REQUIRED \
 	     gnome-icon-theme >= $GNOME_ICON_THEME_REQUIRED \
